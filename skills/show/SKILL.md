---
name: show
description: Show the blueprint summary of a Nova app.
argument-hint: <app_id>
---

Call Nova's `get_app` tool with `app_id: "$ARGUMENTS"`. Present the returned markdown summary verbatim.
